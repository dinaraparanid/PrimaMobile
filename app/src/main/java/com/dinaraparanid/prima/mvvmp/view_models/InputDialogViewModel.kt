package com.dinaraparanid.prima.mvvmp.view_models

import com.dinaraparanid.prima.mvvmp.presenters.InputDialogPresenter

/** [ObservableViewModel] for InputDialog */

class InputDialogViewModel(presenter: InputDialogPresenter) :
    ObservableViewModel<InputDialogPresenter>(presenter)